### YamlMime:UniversalReference
items:
  - uid: '@azure/batch.PoolInformation'
    name: PoolInformation
    fullName: PoolInformation
    children:
      - '@azure/batch.PoolInformation.autoPoolSpecification'
      - '@azure/batch.PoolInformation.poolId'
    langs:
      - typeScript
    type: interface
    summary: <span data-ttu-id="a852d-101">表示 PoolInformation 的接口。</span><span class="sxs-lookup"><span data-stu-id="a852d-101">An interface representing PoolInformation.</span></span>
    package: '@azure/batch'
  - uid: '@azure/batch.PoolInformation.autoPoolSpecification'
    name: autoPoolSpecification
    fullName: autoPoolSpecification
    children: []
    langs:
      - typeScript
    type: property
    summary: |-
      <span data-ttu-id="a852d-102">自动池的临时特征。</span><span class="sxs-lookup"><span data-stu-id="a852d-102">Characteristics for a temporary 'auto pool'.</span></span> <span data-ttu-id="a852d-103">提交作业时，Batch 服务将创建此自动池。</span><span class="sxs-lookup"><span data-stu-id="a852d-103">The Batch service will create this auto pool when the job is submitted.</span></span> <span data-ttu-id="a852d-104">如果自动池创建失败，批处理服务将作业移动到已完成的状态，并在作业的计划错误属性中设置池创建错误。</span><span class="sxs-lookup"><span data-stu-id="a852d-104">If auto pool creation fails, the Batch service moves the job to a completed state, and the pool creation error is set in the job's scheduling error property.</span></span>
      <span data-ttu-id="a852d-105">Batch 服务管理的生存期 (这两种创建和指定保持连接，除非删除) 的自动池。</span><span class="sxs-lookup"><span data-stu-id="a852d-105">The Batch service manages the lifetime (both creation and, unless keepAlive is specified, deletion) of the auto pool.</span></span> <span data-ttu-id="a852d-106">作业处于活动状态时，会影响自动池生存期的任何用户操作将导致意外行为。</span><span class="sxs-lookup"><span data-stu-id="a852d-106">Any user actions that affect the lifetime of the auto pool while the job is active will result in unexpected behavior.</span></span> <span data-ttu-id="a852d-107">必须指定池 ID 或自动池规范，但不可同时使用两者。</span><span class="sxs-lookup"><span data-stu-id="a852d-107">You must specify either the pool ID or the auto pool specification, but not both.</span></span>
    optional: true
    syntax:
      content: 'autoPoolSpecification?: AutoPoolSpecification'
      return:
        type:
          - '@azure/batch.AutoPoolSpecification'
    package: '@azure/batch'
  - uid: '@azure/batch.PoolInformation.poolId'
    name: poolId
    fullName: poolId
    children: []
    langs:
      - typeScript
    type: property
    summary: '<span data-ttu-id="a852d-108">现有池的 ID。</span><span class="sxs-lookup"><span data-stu-id="a852d-108">The ID of an existing pool.</span></span> <span data-ttu-id="a852d-109">作业的所有任务将在指定的池上都运行。</span><span class="sxs-lookup"><span data-stu-id="a852d-109">All the tasks of the job will run on the specified pool.</span></span> <span data-ttu-id="a852d-110">您必须确保此属性所引用的池存在。</span><span class="sxs-lookup"><span data-stu-id="a852d-110">You must ensure that the pool referenced by this property exists.</span></span> <span data-ttu-id="a852d-111">如果池不存在在 Batch 服务会尝试计划作业时，会一直具有该 id 创建池不运行作业的任何任务。请注意，批处理服务将拒绝作业请求;它将只需运行任务之前存在该池。</span><span class="sxs-lookup"><span data-stu-id="a852d-111">If the pool does not exist at the time the Batch service tries to schedule a job, no tasks for the job will run until you create a pool with that id. Note that the Batch service will not reject the job request; it will simply not run tasks until the pool exists.</span></span> <span data-ttu-id="a852d-112">必须指定池 ID 或自动池规范，但不可同时使用两者。</span><span class="sxs-lookup"><span data-stu-id="a852d-112">You must specify either the pool ID or the auto pool specification, but not both.</span></span>'
    optional: true
    syntax:
      content: 'poolId?: undefined | string'
      return:
        type:
          - undefined | string
    package: '@azure/batch'
references:
  - uid: '@azure/batch.AutoPoolSpecification'
    name: AutoPoolSpecification
    spec.typeScript:
      - name: AutoPoolSpecification
        fullName: AutoPoolSpecification
        uid: '@azure/batch.AutoPoolSpecification'